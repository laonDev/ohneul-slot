# "오늘 뭐먹지 슬롯" Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 점심 메뉴를 슬롯머신으로 5초에 정해주는 앱인토스 WebView 미니앱을 빌드해 6/30 챌린지에 제출한다.

**Architecture:** 순수 로직(메뉴 풀 / 확률 가중치 추첨 / 히스토리 집계)은 플랫폼과 무관한 `core/` 모듈로 분리해 Vitest로 TDD한다. 플랫폼 의존 기능(저장소/햅틱/공유/푸시)은 `platform/` 어댑터로 격리해 내부 인터페이스를 고정하고, 실제 호출 시그니처는 설치된 `@apps-in-toss/web-framework` 타입과 예제에서 확인한다. UI는 React 화면 상태 머신(home → result → history)으로 단순하게 구성한다.

**Tech Stack:** React 18 · TypeScript 5.6 · Granite/Rsbuild(`granite dev`/`granite build`) · `@apps-in-toss/web-framework` 1.5.x · Vitest(순수 로직 테스트)

---

## 사전 참고 (구현 전 반드시 확인)

- **로컬 브라우저에서 플랫폼 API는 동작하지 않는다.** 슬롯 연출/저장소/햅틱/공유/푸시는 **샌드박스 앱**에서만 검증된다. 따라서 `platform/` 어댑터는 로컬 개발 중 동작하도록 **localStorage / no-op 폴백**을 갖는다.
- 플랫폼 API의 정확한 호출은 설치 후 `node_modules/@apps-in-toss/web-framework`의 `.d.ts`와 예제(`with-haptic-feedback`, `with-share-text`, `with-share-link`, `with-storage`)에서 확인한다.
- 확인된 사실: `import { Storage } from '@apps-in-toss/web-framework'` → `Storage.getItem(key): Promise<string|null>`, `Storage.setItem(key, value): Promise<void>`. 초기화는 `AppsInToss.registerApp(...)`.

## File Structure

```
오늘-뭐먹지-슬롯/                      # create-ait-app 으로 생성한 프로젝트 루트
├── granite.config.ts                # appName/brand/permissions (콘솔 등록값과 일치)
├── package.json                     # + vitest, 의존성
├── vitest.config.ts                 # 순수 로직 테스트 설정
└── src/
    ├── index.tsx                    # AppsInToss.registerApp 엔트리
    ├── App.tsx                      # 화면 상태 머신(home/result/history)
    ├── index.css
    ├── data/
    │   ├── categories.ts            # 카테고리 목록 (상수)
    │   └── menus.ts                 # 내장 메뉴 풀 (정적 데이터)
    ├── core/                        # 순수 로직 — Vitest TDD
    │   ├── types.ts                 # Menu, HistoryEntry, Settings, Rng
    │   ├── menu.ts                  # getCandidates(category)
    │   ├── history-util.ts          # 주간 집계 / 최근 N일 / 중복 판정
    │   └── picker.ts                # 확률 가중치 추첨
    ├── platform/                    # @apps-in-toss/web-framework 어댑터
    │   ├── storage.ts               # 타입 세이프 JSON get/set (+ 폴백)
    │   ├── haptic.ts                # playHaptic (+ no-op 폴백)
    │   ├── share.ts                 # shareResult (+ 폴백)
    │   └── notify.ts                # 점심 푸시 enable/disable (+ 폴백)
    ├── store/
    │   └── useAppState.ts           # history/favorites/settings 로드·저장 훅
    ├── components/
    │   ├── CategoryPicker.tsx
    │   ├── SlotMachine.tsx          # 릴 애니메이션
    │   ├── ResultCard.tsx
    │   ├── HistoryView.tsx
    │   └── ShareButton.tsx
    └── assets/
        └── reel.mp3                 # 릴 사운드 (선택)
```

각 파일은 단일 책임을 가진다. `core/`는 플랫폼 import가 전혀 없어야 하며(테스트 용이), `platform/`만 `@apps-in-toss/web-framework`를 import한다.

---

## Task 0: 프로젝트 스캐폴딩 & 설정

**Files:**
- Create(생성기): 프로젝트 루트 전체
- Modify: `granite.config.ts`

- [ ] **Step 1: 프로젝트 생성**

작업 폴더(`/Users/kimgyubeom/workspace/toss-challenge`) 안에서 실행:
```bash
npx create-ait-app@latest ohneul-slot
```
대화형 프롬프트: 프레임워크 = React(WebView), TDS = 아니오(게임성 연출이라 불필요), 예제 코드 = 최소/없음.
> 만약 `create-ait-app`이 없으면 `npm create granite-app@latest ohneul-slot` 사용 후 `cd ohneul-slot && npx ait init` 으로 프레임워크/앱이름 설정.

- [ ] **Step 2: 의존성 설치 & 개발 서버 기동 확인**

```bash
cd ohneul-slot
npm install          # 또는 yarn (예제는 yarn@4.9.1)
npm run dev
```
Expected: Granite/Rsbuild 개발 서버가 기동되고 콘솔에 스킴(`intoss://...`)·로컬 URL이 표시됨. (브라우저에서 흰 화면이어도 정상 — 플랫폼 API는 샌드박스 필요)

- [ ] **Step 3: `granite.config.ts`를 앱 정보에 맞게 수정**

`appName: 'ohneul-slot'` 유지. `brand`를 다음과 같이:
```ts
brand: {
  displayName: '오늘 뭐먹지 슬롯',
  primaryColor: '#FF6B35',     // 식욕 도는 오렌지
  icon: 'https://placehold.co/512x512/FF6B35/ffffff?text=%F0%9F%8E%B0', // 임시, 콘솔 등록 후 교체
  bridgeColorMode: 'inverted',
},
```
> `appName`/`displayName`/`icon`은 **앱인토스 콘솔 등록값과 동일**해야 한다(콘솔 등록은 Task 14).

- [ ] **Step 4: 커밋**

```bash
git add -A
git commit -m "chore: scaffold apps-in-toss webview project (ohneul-slot)"
```

---

## Task 1: 테스트 인프라(Vitest) 추가

**Files:**
- Modify: `package.json`
- Create: `vitest.config.ts`, `src/core/__smoke__.test.ts`(임시)

- [ ] **Step 1: Vitest 설치**

```bash
npm install -D vitest
```

- [ ] **Step 2: `vitest.config.ts` 생성**

```ts
import { defineConfig } from 'vitest/config';

export default defineConfig({
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
  },
});
```

- [ ] **Step 3: `package.json`에 test 스크립트 추가**

`scripts`에 추가:
```json
"test": "vitest run",
"test:watch": "vitest"
```

- [ ] **Step 4: 스모크 테스트로 러너 확인**

`src/core/__smoke__.test.ts`:
```ts
import { describe, it, expect } from 'vitest';

describe('smoke', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
```
Run: `npm test`
Expected: PASS (1 passed)

- [ ] **Step 5: 스모크 테스트 삭제 후 커밋**

```bash
rm src/core/__smoke__.test.ts
git add -A
git commit -m "test: add vitest for pure-logic modules"
```

---

## Task 2: 도메인 타입 & 카테고리

**Files:**
- Create: `src/core/types.ts`, `src/data/categories.ts`
- Test: `src/data/categories.test.ts`

- [ ] **Step 1: 타입 정의 작성**

`src/core/types.ts`:
```ts
export type CategoryId =
  | 'all' | 'korean' | 'chinese' | 'western' | 'japanese' | 'bunsik' | 'soloeat' | 'meeting';

export interface Menu {
  id: string;
  name: string;
  emoji: string;
  category: Exclude<CategoryId, 'all'>;
}

export interface HistoryEntry {
  date: string;   // 'YYYY-MM-DD'
  menuId: string;
  category: Exclude<CategoryId, 'all'>;
}

export interface Settings {
  pushEnabled: boolean;
  pushTime: string;       // 'HH:mm'
  lastCategory: CategoryId;
  soundEnabled: boolean;
}

export type Rng = () => number; // [0,1) — 테스트에서 주입 가능
```

- [ ] **Step 2: 실패하는 테스트 작성**

`src/data/categories.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { CATEGORIES } from './categories';

describe('CATEGORIES', () => {
  it('includes "all" first and has unique ids', () => {
    expect(CATEGORIES[0].id).toBe('all');
    const ids = CATEGORIES.map(c => c.id);
    expect(new Set(ids).size).toBe(ids.length);
  });
});
```

- [ ] **Step 3: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL ("Cannot find module './categories'")

- [ ] **Step 4: 카테고리 데이터 작성**

`src/data/categories.ts`:
```ts
import type { CategoryId } from '../core/types';

export interface Category { id: CategoryId; label: string; emoji: string; }

export const CATEGORIES: Category[] = [
  { id: 'all',      label: '전체',   emoji: '🎰' },
  { id: 'korean',   label: '한식',   emoji: '🍚' },
  { id: 'chinese',  label: '중식',   emoji: '🥡' },
  { id: 'western',  label: '양식',   emoji: '🍝' },
  { id: 'japanese', label: '일식',   emoji: '🍣' },
  { id: 'bunsik',   label: '분식',   emoji: '🍢' },
  { id: 'soloeat',  label: '혼밥',   emoji: '🍱' },
  { id: 'meeting',  label: '회식',   emoji: '🍻' },
];
```

- [ ] **Step 5: 테스트 통과 확인 후 커밋**

Run: `npm test`
Expected: PASS
```bash
git add -A
git commit -m "feat: domain types and lunch categories"
```

---

## Task 3: 내장 메뉴 풀

**Files:**
- Create: `src/data/menus.ts`
- Test: `src/data/menus.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/data/menus.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { MENUS } from './menus';
import { CATEGORIES } from './categories';

const validCats = new Set(CATEGORIES.map(c => c.id).filter(id => id !== 'all'));

describe('MENUS', () => {
  it('has at least 80 menus', () => {
    expect(MENUS.length).toBeGreaterThanOrEqual(80);
  });
  it('all ids are unique', () => {
    const ids = MENUS.map(m => m.id);
    expect(new Set(ids).size).toBe(ids.length);
  });
  it('every menu has a valid category, name, and emoji', () => {
    for (const m of MENUS) {
      expect(validCats.has(m.category)).toBe(true);
      expect(m.name.length).toBeGreaterThan(0);
      expect(m.emoji.length).toBeGreaterThan(0);
    }
  });
  it('every non-"all" category has at least 5 menus', () => {
    for (const cat of validCats) {
      expect(MENUS.filter(m => m.category === cat).length).toBeGreaterThanOrEqual(5);
    }
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL ("Cannot find module './menus'")

- [ ] **Step 3: 메뉴 풀 작성 (시드 ~90개, 같은 패턴으로 250까지 확장 가능)**

`src/data/menus.ts` — 아래는 카테고리별 시드. **데이터 입력 작업**이므로 같은 `{ id, name, emoji, category }` 형태로 각 카테고리를 늘려 총 200~250개까지 채운다(메뉴 추가는 기계적 작업, 로직 변경 없음).
```ts
import type { Menu } from '../core/types';

export const MENUS: Menu[] = [
  // 한식
  { id: 'k01', name: '김치찌개',   emoji: '🍲', category: 'korean' },
  { id: 'k02', name: '된장찌개',   emoji: '🥘', category: 'korean' },
  { id: 'k03', name: '제육볶음',   emoji: '🍖', category: 'korean' },
  { id: 'k04', name: '국밥',       emoji: '🍜', category: 'korean' },
  { id: 'k05', name: '비빔밥',     emoji: '🍚', category: 'korean' },
  { id: 'k06', name: '불고기',     emoji: '🥩', category: 'korean' },
  { id: 'k07', name: '순두부찌개', emoji: '🍲', category: 'korean' },
  { id: 'k08', name: '갈비탕',     emoji: '🍲', category: 'korean' },
  { id: 'k09', name: '냉면',       emoji: '🥶', category: 'korean' },
  { id: 'k10', name: '김치볶음밥', emoji: '🍳', category: 'korean' },
  { id: 'k11', name: '돈까스',     emoji: '🍱', category: 'korean' },
  { id: 'k12', name: '닭갈비',     emoji: '🐔', category: 'korean' },
  // 중식
  { id: 'c01', name: '짜장면',     emoji: '🍜', category: 'chinese' },
  { id: 'c02', name: '짬뽕',       emoji: '🌶️', category: 'chinese' },
  { id: 'c03', name: '탕수육',     emoji: '🍤', category: 'chinese' },
  { id: 'c04', name: '마라탕',     emoji: '🥵', category: 'chinese' },
  { id: 'c05', name: '볶음밥',     emoji: '🍚', category: 'chinese' },
  { id: 'c06', name: '울면',       emoji: '🍜', category: 'chinese' },
  { id: 'c07', name: '깐풍기',     emoji: '🍗', category: 'chinese' },
  { id: 'c08', name: '양장피',     emoji: '🥗', category: 'chinese' },
  // 양식
  { id: 'w01', name: '파스타',     emoji: '🍝', category: 'western' },
  { id: 'w02', name: '피자',       emoji: '🍕', category: 'western' },
  { id: 'w03', name: '햄버거',     emoji: '🍔', category: 'western' },
  { id: 'w04', name: '스테이크',   emoji: '🥩', category: 'western' },
  { id: 'w05', name: '리조또',     emoji: '🍚', category: 'western' },
  { id: 'w06', name: '샐러드',     emoji: '🥗', category: 'western' },
  { id: 'w07', name: '오므라이스', emoji: '🍳', category: 'western' },
  { id: 'w08', name: '브런치',     emoji: '🥞', category: 'western' },
  // 일식
  { id: 'j01', name: '초밥',       emoji: '🍣', category: 'japanese' },
  { id: 'j02', name: '라멘',       emoji: '🍜', category: 'japanese' },
  { id: 'j03', name: '규동',       emoji: '🍱', category: 'japanese' },
  { id: 'j04', name: '우동',       emoji: '🍲', category: 'japanese' },
  { id: 'j05', name: '돈부리',     emoji: '🍚', category: 'japanese' },
  { id: 'j06', name: '카레',       emoji: '🍛', category: 'japanese' },
  { id: 'j07', name: '텐동',       emoji: '🍤', category: 'japanese' },
  // 분식
  { id: 'b01', name: '떡볶이',     emoji: '🍢', category: 'bunsik' },
  { id: 'b02', name: '김밥',       emoji: '🍙', category: 'bunsik' },
  { id: 'b03', name: '라면',       emoji: '🍜', category: 'bunsik' },
  { id: 'b04', name: '순대',       emoji: '🌭', category: 'bunsik' },
  { id: 'b05', name: '튀김',       emoji: '🍤', category: 'bunsik' },
  { id: 'b06', name: '쫄면',       emoji: '🍝', category: 'bunsik' },
  // 혼밥
  { id: 's01', name: '편의점도시락', emoji: '🍱', category: 'soloeat' },
  { id: 's02', name: '샌드위치',   emoji: '🥪', category: 'soloeat' },
  { id: 's03', name: '국수',       emoji: '🍜', category: 'soloeat' },
  { id: 's04', name: '백반',       emoji: '🍚', category: 'soloeat' },
  { id: 's05', name: '죽',         emoji: '🥣', category: 'soloeat' },
  { id: 's06', name: '토스트',     emoji: '🍞', category: 'soloeat' },
  // 회식
  { id: 'm01', name: '삼겹살',     emoji: '🥓', category: 'meeting' },
  { id: 'm02', name: '치킨',       emoji: '🍗', category: 'meeting' },
  { id: 'm03', name: '곱창',       emoji: '🍖', category: 'meeting' },
  { id: 'm04', name: '횟집',       emoji: '🐟', category: 'meeting' },
  { id: 'm05', name: '족발',       emoji: '🐷', category: 'meeting' },
  { id: 'm06', name: '보쌈',       emoji: '🥬', category: 'meeting' },
];
```

- [ ] **Step 4: 테스트 통과 확인 후 커밋**

Run: `npm test`
Expected: PASS (메뉴 80개 미만이면 각 카테고리에 항목을 더 추가)
```bash
git add -A
git commit -m "feat: built-in lunch menu pool"
```

---

## Task 4: 히스토리 유틸 (집계/최근/중복)

**Files:**
- Create: `src/core/history-util.ts`
- Test: `src/core/history-util.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/core/history-util.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { daysSince, recentMenuIds, weekEntries, alreadyEatenToday } from './history-util';
import type { HistoryEntry } from './types';

const H: HistoryEntry[] = [
  { date: '2026-06-08', menuId: 'k04', category: 'korean' },
  { date: '2026-06-09', menuId: 'c01', category: 'chinese' },
];

describe('daysSince', () => {
  it('returns whole-day difference', () => {
    expect(daysSince('2026-06-08', '2026-06-09')).toBe(1);
    expect(daysSince('2026-06-09', '2026-06-09')).toBe(0);
  });
});

describe('recentMenuIds', () => {
  it('returns ids eaten within N days of "today"', () => {
    expect(recentMenuIds(H, '2026-06-09', 1)).toEqual(['k04', 'c01']);
    expect(recentMenuIds(H, '2026-06-09', 0)).toEqual(['c01']);
  });
});

describe('weekEntries', () => {
  it('returns entries in the same ISO week (Mon start) as the given date', () => {
    // 2026-06-08 is Monday; 2026-06-09 Tuesday → same week
    expect(weekEntries(H, '2026-06-09').length).toBe(2);
    expect(weekEntries(H, '2026-06-15').length).toBe(0); // next week
  });
});

describe('alreadyEatenToday', () => {
  it('detects an entry already logged for the date', () => {
    expect(alreadyEatenToday(H, '2026-06-09')).toBe(true);
    expect(alreadyEatenToday(H, '2026-06-10')).toBe(false);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL ("Cannot find module './history-util'")

- [ ] **Step 3: 구현 작성**

`src/core/history-util.ts`:
```ts
import type { HistoryEntry } from './types';

const MS_PER_DAY = 24 * 60 * 60 * 1000;

function toUTC(date: string): number {
  const [y, m, d] = date.split('-').map(Number);
  return Date.UTC(y, m - 1, d);
}

export function daysSince(from: string, to: string): number {
  return Math.round((toUTC(to) - toUTC(from)) / MS_PER_DAY);
}

/** today 기준 N일 이내(0 = 오늘만)에 먹은 메뉴 id 목록 */
export function recentMenuIds(history: HistoryEntry[], today: string, days: number): string[] {
  return history
    .filter(e => { const d = daysSince(e.date, today); return d >= 0 && d <= days; })
    .map(e => e.menuId);
}

/** 월요일 시작 주의 시작 날짜('YYYY-MM-DD') */
function mondayOf(date: string): number {
  const t = toUTC(date);
  const dow = new Date(t).getUTCDay();        // 0=Sun..6=Sat
  const offset = (dow + 6) % 7;               // Mon=0
  return t - offset * MS_PER_DAY;
}

export function weekEntries(history: HistoryEntry[], date: string): HistoryEntry[] {
  const start = mondayOf(date);
  const end = start + 7 * MS_PER_DAY;
  return history.filter(e => { const t = toUTC(e.date); return t >= start && t < end; });
}

export function alreadyEatenToday(history: HistoryEntry[], today: string): boolean {
  return history.some(e => e.date === today);
}
```

- [ ] **Step 4: 테스트 통과 확인 후 커밋**

Run: `npm test`
Expected: PASS
```bash
git add -A
git commit -m "feat: history utilities (recency, weekly, dedupe)"
```

---

## Task 5: 확률 가중치 추첨 엔진

**Files:**
- Create: `src/core/picker.ts`
- Test: `src/core/picker.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/core/picker.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { menuWeight, weightedPick } from './picker';
import type { Menu, HistoryEntry } from './types';

const M = (id: string): Menu => ({ id, name: id, emoji: '🍚', category: 'korean' });
const candidates = [M('a'), M('b'), M('c')];

describe('menuWeight', () => {
  it('is 1 when never eaten and not favorite', () => {
    expect(menuWeight(M('a'), [], new Set(), '2026-06-09')).toBeCloseTo(1);
  });
  it('drops sharply when eaten yesterday', () => {
    const h: HistoryEntry[] = [{ date: '2026-06-08', menuId: 'a', category: 'korean' }];
    expect(menuWeight(M('a'), h, new Set(), '2026-06-09')).toBeLessThan(0.2);
  });
  it('recovers fully after 7+ days', () => {
    const h: HistoryEntry[] = [{ date: '2026-06-01', menuId: 'a', category: 'korean' }];
    expect(menuWeight(M('a'), h, new Set(), '2026-06-09')).toBeCloseTo(1);
  });
  it('boosts favorites', () => {
    const w = menuWeight(M('a'), [], new Set(['a']), '2026-06-09');
    expect(w).toBeGreaterThan(1);
  });
});

describe('weightedPick', () => {
  it('returns null for empty candidates', () => {
    expect(weightedPick([], [], new Set(), '2026-06-09', () => 0.5)).toBeNull();
  });
  it('is deterministic for a given rng value', () => {
    // 모두 weight 1 → 누적 [1,2,3], rng=0.5 → 0.5*3=1.5 → 두 번째(b)
    const picked = weightedPick(candidates, [], new Set(), '2026-06-09', () => 0.5);
    expect(picked?.id).toBe('b');
  });
  it('never returns a zero-weight-only impossible pick', () => {
    const picked = weightedPick(candidates, [], new Set(), '2026-06-09', () => 0.999999);
    expect(picked?.id).toBe('c');
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL ("Cannot find module './picker'")

- [ ] **Step 3: 구현 작성**

`src/core/picker.ts`:
```ts
import type { Menu, HistoryEntry, Rng } from './types';
import { daysSince } from './history-util';

const FAVORITE_BOOST = 1.3;

/** 최근에 먹었을수록 0에 가깝게, 7일 이상이면 1.0으로 회복 */
function recencyDecay(menu: Menu, history: HistoryEntry[], today: string): number {
  let decay = 1;
  for (const e of history) {
    if (e.menuId !== menu.id) continue;
    const d = daysSince(e.date, today);
    if (d < 0 || d >= 7) continue;
    // d=0 → 0.05, d=1 → ~0.1, d=3 → ~0.5, d=6 → ~0.9
    const factor = 0.05 + (d / 7) * 0.95;
    decay = Math.min(decay, factor);
  }
  return decay;
}

export function menuWeight(
  menu: Menu, history: HistoryEntry[], favorites: Set<string>, today: string,
): number {
  const base = 1;
  const fav = favorites.has(menu.id) ? FAVORITE_BOOST : 1;
  return base * recencyDecay(menu, history, today) * fav;
}

export function weightedPick(
  candidates: Menu[], history: HistoryEntry[], favorites: Set<string>, today: string, rng: Rng,
): Menu | null {
  if (candidates.length === 0) return null;
  const weights = candidates.map(m => Math.max(menuWeight(m, history, favorites, today), 0.0001));
  const total = weights.reduce((a, b) => a + b, 0);
  let r = rng() * total;
  for (let i = 0; i < candidates.length; i++) {
    r -= weights[i];
    if (r < 0) return candidates[i];
  }
  return candidates[candidates.length - 1];
}
```

- [ ] **Step 4: 테스트 통과 확인 후 커밋**

Run: `npm test`
Expected: PASS
```bash
git add -A
git commit -m "feat: weighted lunch picker (recency decay + favorite boost)"
```

---

## Task 6: menu 모듈 (카테고리 필터)

**Files:**
- Create: `src/core/menu.ts`
- Test: `src/core/menu.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

`src/core/menu.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { getCandidates } from './menu';
import { MENUS } from '../data/menus';

describe('getCandidates', () => {
  it('returns all menus for "all"', () => {
    expect(getCandidates('all').length).toBe(MENUS.length);
  });
  it('returns only matching category', () => {
    const korean = getCandidates('korean');
    expect(korean.length).toBeGreaterThan(0);
    expect(korean.every(m => m.category === 'korean')).toBe(true);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL ("Cannot find module './menu'")

- [ ] **Step 3: 구현 작성**

`src/core/menu.ts`:
```ts
import type { Menu, CategoryId } from './types';
import { MENUS } from '../data/menus';

export function getCandidates(category: CategoryId): Menu[] {
  if (category === 'all') return MENUS;
  return MENUS.filter(m => m.category === category);
}
```

- [ ] **Step 4: 테스트 통과 확인 후 커밋**

Run: `npm test`
Expected: PASS
```bash
git add -A
git commit -m "feat: category filter for candidates"
```

---

## Task 7: 저장소 어댑터 (타입 세이프 JSON + 폴백)

**Files:**
- Create: `src/platform/storage.ts`
- Test: `src/platform/storage.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성 (폴백 경로 검증)**

`src/platform/storage.test.ts` — 테스트 환경(node)에서는 폴백(in-memory)이 동작해야 한다.
```ts
import { describe, it, expect } from 'vitest';
import { getJSON, setJSON } from './storage';

describe('storage JSON helpers (fallback path)', () => {
  it('round-trips a value', async () => {
    await setJSON('unit:test', { a: 1 });
    expect(await getJSON<{ a: number }>('unit:test', { a: 0 })).toEqual({ a: 1 });
  });
  it('returns fallback for missing key', async () => {
    expect(await getJSON('unit:missing', 'def')).toBe('def');
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL ("Cannot find module './storage'")

- [ ] **Step 3: 구현 작성**

`src/platform/storage.ts` — 플랫폼 `Storage`가 없으면(node/로컬) in-memory/localStorage 폴백.
```ts
// 확인된 API: import { Storage } from '@apps-in-toss/web-framework'
//   Storage.getItem(key): Promise<string|null>; Storage.setItem(key, value): Promise<void>
let platformStorage: {
  getItem(k: string): Promise<string | null>;
  setItem(k: string, v: string): Promise<void>;
} | null = null;

try {
  // 동적 require로 node 테스트에서 import 실패를 폴백 처리
  // eslint-disable-next-line @typescript-eslint/no-var-requires
  const mod = require('@apps-in-toss/web-framework');
  if (mod?.Storage?.getItem) platformStorage = mod.Storage;
} catch { /* 로컬/테스트: 폴백 사용 */ }

const mem = new Map<string, string>();
const fallback = {
  async getItem(k: string) {
    if (typeof localStorage !== 'undefined') return localStorage.getItem(k);
    return mem.has(k) ? mem.get(k)! : null;
  },
  async setItem(k: string, v: string) {
    if (typeof localStorage !== 'undefined') localStorage.setItem(k, v);
    else mem.set(k, v);
  },
};

function impl() { return platformStorage ?? fallback; }

export async function getJSON<T>(key: string, fallbackValue: T): Promise<T> {
  const raw = await impl().getItem(key);
  if (raw == null) return fallbackValue;
  try { return JSON.parse(raw) as T; } catch { return fallbackValue; }
}

export async function setJSON<T>(key: string, value: T): Promise<void> {
  await impl().setItem(key, JSON.stringify(value));
}
```
> **샌드박스 검증 시**: 실제 기기/샌드박스에서는 `@apps-in-toss/web-framework`의 `Storage`가 바인딩되어 영구 저장된다. require가 번들에서 동작하지 않으면 상단을 정적 `import { Storage } from '@apps-in-toss/web-framework'` + `typeof Storage !== 'undefined'` 가드로 교체.

- [ ] **Step 4: 테스트 통과 확인 후 커밋**

Run: `npm test`
Expected: PASS
```bash
git add -A
git commit -m "feat: type-safe storage adapter with local fallback"
```

---

## Task 8: 앱 상태 훅 (history/favorites/settings)

**Files:**
- Create: `src/store/useAppState.ts`
- Test: 없음(샌드박스 통합 검증) — 순수 변환은 Task 4/5에서 커버

- [ ] **Step 1: 구현 작성**

`src/store/useAppState.ts`:
```ts
import { useEffect, useState, useCallback } from 'react';
import { getJSON, setJSON } from '../platform/storage';
import type { HistoryEntry, Settings, CategoryId } from '../core/types';

const K_HISTORY = 'ohneul:history';
const K_FAV = 'ohneul:favorites';
const K_SETTINGS = 'ohneul:settings';

const DEFAULT_SETTINGS: Settings = {
  pushEnabled: false, pushTime: '11:30', lastCategory: 'all', soundEnabled: true,
};

export function useAppState() {
  const [history, setHistory] = useState<HistoryEntry[]>([]);
  const [favorites, setFavorites] = useState<string[]>([]);
  const [settings, setSettings] = useState<Settings>(DEFAULT_SETTINGS);
  const [loaded, setLoaded] = useState(false);

  useEffect(() => {
    (async () => {
      setHistory(await getJSON<HistoryEntry[]>(K_HISTORY, []));
      setFavorites(await getJSON<string[]>(K_FAV, []));
      setSettings(await getJSON<Settings>(K_SETTINGS, DEFAULT_SETTINGS));
      setLoaded(true);
    })();
  }, []);

  const addHistory = useCallback((entry: HistoryEntry) => {
    setHistory(prev => { const next = [...prev, entry]; setJSON(K_HISTORY, next); return next; });
  }, []);

  const toggleFavorite = useCallback((menuId: string) => {
    setFavorites(prev => {
      const next = prev.includes(menuId) ? prev.filter(id => id !== menuId) : [...prev, menuId];
      setJSON(K_FAV, next); return next;
    });
  }, []);

  const updateSettings = useCallback((patch: Partial<Settings>) => {
    setSettings(prev => { const next = { ...prev, ...patch }; setJSON(K_SETTINGS, next); return next; });
  }, []);

  return { loaded, history, favorites, settings, addHistory, toggleFavorite, updateSettings };
}
```

- [ ] **Step 2: 타입체크 후 커밋**

Run: `npx tsc --noEmit`
Expected: 에러 없음
```bash
git add -A
git commit -m "feat: app state hook for history/favorites/settings"
```

---

## Task 9: 햅틱 어댑터

**Files:**
- Create: `src/platform/haptic.ts`

- [ ] **Step 1: 실제 햅틱 API 확인**

`with-haptic-feedback` 예제(`https://github.com/toss/apps-in-toss-examples/tree/main/with-haptic-feedback`)와 `node_modules/@apps-in-toss/web-framework`의 타입에서 햅틱 함수명을 확인한다(예상: `generateHapticFeedback({ type })` 또는 유사).

- [ ] **Step 2: 어댑터 작성 (확인한 함수명으로 채움 + no-op 폴백)**

`src/platform/haptic.ts`:
```ts
// 실제 함수명은 Step 1에서 확인해 채운다. 아래는 격리된 어댑터 형태.
type HapticKind = 'tick' | 'success';

let raw: ((kind: HapticKind) => void) | null = null;
try {
  // eslint-disable-next-line @typescript-eslint/no-var-requires
  const mod = require('@apps-in-toss/web-framework');
  // 예: mod.generateHapticFeedback?.({ type: 'tickWeak' })
  if (mod?.generateHapticFeedback) {
    raw = (kind) => mod.generateHapticFeedback({ type: kind === 'success' ? 'success' : 'tickWeak' });
  }
} catch { /* 폴백 */ }

export function playHaptic(kind: HapticKind = 'tick') {
  if (raw) { raw(kind); return; }
  if (typeof navigator !== 'undefined' && 'vibrate' in navigator) {
    navigator.vibrate(kind === 'success' ? [30, 40, 30] : 10); // 웹 폴백
  }
}
```

- [ ] **Step 3: 타입체크 후 커밋**

Run: `npx tsc --noEmit`
Expected: 에러 없음
```bash
git add -A
git commit -m "feat: haptic adapter with web vibration fallback"
```

---

## Task 10: 슬롯머신 컴포넌트 (릴 연출)

**Files:**
- Create: `src/components/SlotMachine.tsx`

- [ ] **Step 1: 컴포넌트 작성**

`src/components/SlotMachine.tsx` — 당첨 메뉴를 받아 3릴이 순차 정지하는 CSS 트랜지션 연출. 각 릴 정지 시 `playHaptic('tick')`, 최종 정지 시 `playHaptic('success')`.
```tsx
import { useEffect, useRef, useState } from 'react';
import type { Menu } from '../core/types';
import { playHaptic } from '../platform/haptic';

const STRIP_EMOJIS = ['🍚','🍜','🍕','🍣','🍔','🍱','🍢','🥘','🍗','🍙'];
const ROW_H = 64;     // px
const REEL_STOP_MS = [700, 1000, 1300];

interface Props { result: Menu | null; spinning: boolean; onSpinEnd: () => void; }

export function SlotMachine({ result, spinning, onSpinEnd }: Props) {
  const [offsets, setOffsets] = useState([0, 0, 0]);
  const endedRef = useRef(false);

  useEffect(() => {
    if (!spinning || !result) return;
    endedRef.current = false;
    // 빠르게 돌다 순차 정지하는 느낌: 각 릴을 큰 오프셋으로 보냈다가 정지
    const spins = [40, 46, 52].map(n => n * ROW_H);
    setOffsets(spins);
    const timers = REEL_STOP_MS.map((ms, i) =>
      setTimeout(() => {
        playHaptic('tick');
        if (i === 2) {
          playHaptic('success');
          if (!endedRef.current) { endedRef.current = true; onSpinEnd(); }
        }
      }, ms),
    );
    return () => timers.forEach(clearTimeout);
  }, [spinning, result, onSpinEnd]);

  return (
    <div className="slot">
      {[0, 1, 2].map(i => (
        <div className="reel" key={i} style={{ height: ROW_H * 3, overflow: 'hidden' }}>
          <div
            className="reel-strip"
            style={{
              transform: `translateY(-${offsets[i]}px)`,
              transition: spinning ? `transform ${REEL_STOP_MS[i]}ms cubic-bezier(.2,.8,.2,1)` : 'none',
            }}
          >
            {/* 마지막 칸은 당첨 이모지로 보이게 */}
            {[...Array(60)].map((_, r) => (
              <div className="reel-cell" key={r} style={{ height: ROW_H }}>
                {r === 59 && result ? result.emoji : STRIP_EMOJIS[(r + i) % STRIP_EMOJIS.length]}
              </div>
            ))}
          </div>
        </div>
      ))}
    </div>
  );
}
```
> 연출 디테일(정지 위치 정렬, blur, 사운드 재생)은 샌드박스에서 손맛을 보며 튜닝한다. 핵심은 "순차 정지 + 햅틱"이라는 구조.

- [ ] **Step 2: 타입체크 후 커밋**

Run: `npx tsc --noEmit`
Expected: 에러 없음
```bash
git add -A
git commit -m "feat: slot machine reel component"
```

---

## Task 11: 카테고리 선택 + 결과 카드 컴포넌트

**Files:**
- Create: `src/components/CategoryPicker.tsx`, `src/components/ResultCard.tsx`

- [ ] **Step 1: CategoryPicker 작성**

`src/components/CategoryPicker.tsx`:
```tsx
import type { CategoryId } from '../core/types';
import { CATEGORIES } from '../data/categories';

interface Props { value: CategoryId; onChange: (id: CategoryId) => void; }

export function CategoryPicker({ value, onChange }: Props) {
  return (
    <div className="category-row">
      {CATEGORIES.map(c => (
        <button
          key={c.id}
          className={`chip ${value === c.id ? 'chip--on' : ''}`}
          onClick={() => onChange(c.id)}
        >
          <span>{c.emoji}</span> {c.label}
        </button>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: ResultCard 작성**

`src/components/ResultCard.tsx`:
```tsx
import type { Menu } from '../core/types';

interface Props {
  menu: Menu;
  isFavorite: boolean;
  onEat: () => void;
  onRespin: () => void;
  onToggleFav: () => void;
  shareSlot: React.ReactNode;
}

export function ResultCard({ menu, isFavorite, onEat, onRespin, onToggleFav, shareSlot }: Props) {
  return (
    <div className="result-card">
      <div className="result-emoji">{menu.emoji}</div>
      <div className="result-name">오늘은 <b>{menu.name}</b>!</div>
      <div className="result-actions">
        <button onClick={onEat}>먹었어요 기록</button>
        <button onClick={onRespin}>다시 돌리기</button>
        <button onClick={onToggleFav}>{isFavorite ? '⭐ 단골' : '☆ 단골 등록'}</button>
      </div>
      {shareSlot}
    </div>
  );
}
```

- [ ] **Step 3: 타입체크 후 커밋**

Run: `npx tsc --noEmit`
Expected: 에러 없음
```bash
git add -A
git commit -m "feat: category picker and result card"
```

---

## Task 12: 공유 어댑터 + 공유 버튼

**Files:**
- Create: `src/platform/share.ts`, `src/components/ShareButton.tsx`

- [ ] **Step 1: 실제 공유 API 확인**

`with-share-text` / `with-share-link` 예제와 프레임워크 타입에서 공유 함수명을 확인한다(예상: `share({ message })` 또는 `shareLink(...)`).

- [ ] **Step 2: 공유 어댑터 작성**

`src/platform/share.ts`:
```ts
import type { Menu } from '../core/types';

const APP_LINK = 'https://apps-in-toss.toss.im/'; // 콘솔 발급 미니앱 링크로 교체

let rawShare: ((message: string) => Promise<void>) | null = null;
try {
  // eslint-disable-next-line @typescript-eslint/no-var-requires
  const mod = require('@apps-in-toss/web-framework');
  // 예: mod.share?.({ message }) 또는 mod.shareText?.(message) — Step 1에서 확정
  if (mod?.share) rawShare = (m: string) => mod.share({ message: m });
} catch { /* 폴백 */ }

export async function shareResult(menu: Menu): Promise<void> {
  const message = `오늘 점심은 "${menu.name}" ${menu.emoji}\n나도 슬롯 돌려보기 👉 ${APP_LINK}`;
  if (rawShare) { await rawShare(message); return; }
  if (typeof navigator !== 'undefined' && (navigator as any).share) {
    await (navigator as any).share({ text: message }); // 웹 폴백
    return;
  }
  if (typeof navigator !== 'undefined' && navigator.clipboard) {
    await navigator.clipboard.writeText(message); // 최종 폴백: 클립보드
  }
}
```

- [ ] **Step 3: ShareButton 작성**

`src/components/ShareButton.tsx`:
```tsx
import type { Menu } from '../core/types';
import { shareResult } from '../platform/share';

export function ShareButton({ menu }: { menu: Menu }) {
  return (
    <button className="share-btn" onClick={() => shareResult(menu)}>
      결과 공유하기 📤
    </button>
  );
}
```

- [ ] **Step 4: 타입체크 후 커밋**

Run: `npx tsc --noEmit`
Expected: 에러 없음
```bash
git add -A
git commit -m "feat: share adapter and share button"
```

---

## Task 13: 히스토리 화면

**Files:**
- Create: `src/components/HistoryView.tsx`

- [ ] **Step 1: 컴포넌트 작성**

`src/components/HistoryView.tsx` — 이번 주 먹은 기록을 요일별로 표시.
```tsx
import type { HistoryEntry } from '../core/types';
import { MENUS } from '../data/menus';
import { weekEntries } from '../core/history-util';

const DOW = ['월','화','수','목','금','토','일'];

function menuName(id: string) { return MENUS.find(m => m.id === id)?.name ?? '?'; }
function menuEmoji(id: string) { return MENUS.find(m => m.id === id)?.emoji ?? '🍽️'; }

export function HistoryView({ history, today, onClose }:
  { history: HistoryEntry[]; today: string; onClose: () => void }) {
  const week = weekEntries(history, today);
  const byDate = new Map(week.map(e => [e.date, e]));
  const [y, m, d] = today.split('-').map(Number);
  const base = Date.UTC(y, m - 1, d);
  const monday = base - ((new Date(base).getUTCDay() + 6) % 7) * 86400000;

  return (
    <div className="history-view">
      <header><h2>이번 주 점심</h2><button onClick={onClose}>닫기</button></header>
      <ul>
        {DOW.map((label, i) => {
          const dt = new Date(monday + i * 86400000).toISOString().slice(0, 10);
          const e = byDate.get(dt);
          return (
            <li key={dt} className={dt === today ? 'today' : ''}>
              <span className="dow">{label}</span>
              <span>{e ? `${menuEmoji(e.menuId)} ${menuName(e.menuId)}` : '—'}</span>
            </li>
          );
        })}
      </ul>
    </div>
  );
}
```

- [ ] **Step 2: 타입체크 후 커밋**

Run: `npx tsc --noEmit`
Expected: 에러 없음
```bash
git add -A
git commit -m "feat: weekly history view"
```

---

## Task 14: 푸시 어댑터 + 설정 토글

**Files:**
- Create: `src/platform/notify.ts`

- [ ] **Step 1: 푸시 방식 확인 (개발자센터 + 콘솔)**

`https://developers-apps-in-toss.toss.im/push/intro.html` / `.../push/develop.html` 에서 푸시 발송 방식을 확인한다. 푸시는 **콘솔 "Push & Notification"에서 발송**하는 구조일 가능성이 높다 → 앱은 **권한 등록/수신 동의**만 담당. `granite.config.ts`의 `permissions`에 필요한 푸시 권한을 추가한다.

- [ ] **Step 2: notify 어댑터 작성**

`src/platform/notify.ts` — 앱 측은 동의 상태 관리. 실제 정기 발송은 콘솔/플랫폼 스케줄에 의존.
```ts
let rawEnable: (() => Promise<boolean>) | null = null;
try {
  // eslint-disable-next-line @typescript-eslint/no-var-requires
  const mod = require('@apps-in-toss/web-framework');
  // 예: mod.requestNotificationPermission?.() — Step 1에서 확정
  if (mod?.requestNotificationPermission) {
    rawEnable = () => mod.requestNotificationPermission();
  }
} catch { /* 폴백 */ }

/** 점심 알림 수신 동의 요청. 성공 시 true. (정기 발송은 콘솔/플랫폼) */
export async function enableLunchPush(): Promise<boolean> {
  if (rawEnable) { try { return await rawEnable(); } catch { return false; } }
  return false; // 로컬 폴백
}
```
> 만약 앱 코드에서 로컬 노티 스케줄이 가능하면(프레임워크 타입 확인) `scheduleLocalNotification` 류로 평일 11:30 알림을 등록하도록 확장한다.

- [ ] **Step 3: 타입체크 후 커밋**

Run: `npx tsc --noEmit`
Expected: 에러 없음
```bash
git add -A
git commit -m "feat: lunch push opt-in adapter"
```

---

## Task 15: App 화면 조립 + 스타일

**Files:**
- Modify: `src/App.tsx`, `src/index.tsx`, `src/index.css`

- [ ] **Step 1: App.tsx 작성 (home → result → history 상태 머신)**

`src/App.tsx`:
```tsx
import { useMemo, useState } from 'react';
import { useAppState } from './store/useAppState';
import { getCandidates } from './core/menu';
import { weightedPick } from './core/picker';
import { alreadyEatenToday } from './core/history-util';
import { CategoryPicker } from './components/CategoryPicker';
import { SlotMachine } from './components/SlotMachine';
import { ResultCard } from './components/ResultCard';
import { ShareButton } from './components/ShareButton';
import { HistoryView } from './components/HistoryView';
import type { CategoryId, Menu } from './core/types';

function todayStr(): string { return new Date().toISOString().slice(0, 10); }

export default function App() {
  const { loaded, history, favorites, settings, addHistory, toggleFavorite, updateSettings } = useAppState();
  const [category, setCategory] = useState<CategoryId>('all');
  const [spinning, setSpinning] = useState(false);
  const [result, setResult] = useState<Menu | null>(null);
  const [showHistory, setShowHistory] = useState(false);
  const today = todayStr();
  const favSet = useMemo(() => new Set(favorites), [favorites]);

  function spin() {
    const candidates = getCandidates(category);
    const picked = weightedPick(candidates, history, favSet, today, Math.random);
    setResult(picked);
    setSpinning(true);
  }

  if (!loaded) return <div className="loading">불러오는 중…</div>;

  return (
    <div className="app">
      <header className="app-header">
        <h1>오늘 뭐먹지 슬롯</h1>
        <button onClick={() => setShowHistory(true)}>📅</button>
      </header>

      {!result && (
        <>
          <CategoryPicker value={category} onChange={setCategory} />
          <button className="spin-btn" onClick={spin} disabled={spinning}>돌리기 🎰</button>
        </>
      )}

      <SlotMachine result={result} spinning={spinning} onSpinEnd={() => setSpinning(false)} />

      {result && !spinning && (
        <ResultCard
          menu={result}
          isFavorite={favSet.has(result.id)}
          onEat={() => { if (!alreadyEatenToday(history, today)) addHistory({ date: today, menuId: result.id, category: result.category }); setResult(null); }}
          onRespin={() => { setResult(null); spin(); }}
          onToggleFav={() => toggleFavorite(result.id)}
          shareSlot={<ShareButton menu={result} />}
        />
      )}

      {showHistory && (
        <HistoryView history={history} today={today} onClose={() => setShowHistory(false)} />
      )}
    </div>
  );
}
```
> `updateSettings`/`settings`는 푸시 토글 UI(후속)에서 사용한다. 미사용 경고를 피하려면 이 단계에서 간단한 설정 토글을 헤더에 추가하거나 `void settings` 처리.

- [ ] **Step 2: 엔트리(index.tsx) — registerApp 연결**

`src/index.tsx`는 `create-ait-app` 생성물 기준으로 `AppsInToss.registerApp`/루트 렌더가 이미 있을 수 있다. 없으면 예제(`weekly-todo-react/src/index.tsx`)를 참고해 `App`을 마운트한다. 핵심: 최상위에서 `App`이 렌더되도록 보장.

- [ ] **Step 3: 기본 스타일 작성**

`src/index.css` — 슬롯/칩/결과카드 최소 스타일(아래는 시작점, 샌드박스에서 다듬음):
```css
:root { --brand: #FF6B35; }
* { box-sizing: border-box; }
body { margin: 0; font-family: -apple-system, system-ui, sans-serif; }
.app { max-width: 480px; margin: 0 auto; padding: 16px; }
.app-header { display: flex; justify-content: space-between; align-items: center; }
.category-row { display: flex; gap: 8px; overflow-x: auto; padding: 12px 0; }
.chip { border: 1px solid #ddd; background: #fff; border-radius: 999px; padding: 8px 12px; white-space: nowrap; }
.chip--on { background: var(--brand); color: #fff; border-color: var(--brand); }
.spin-btn { width: 100%; padding: 16px; font-size: 18px; font-weight: 700; color: #fff; background: var(--brand); border: 0; border-radius: 16px; }
.slot { display: flex; gap: 8px; justify-content: center; margin: 24px 0; }
.reel { width: 72px; border: 2px solid #111; border-radius: 12px; background: #fff; }
.reel-cell { display: flex; align-items: center; justify-content: center; font-size: 36px; }
.result-card { text-align: center; padding: 24px; }
.result-emoji { font-size: 72px; }
.result-name { font-size: 22px; margin: 12px 0; }
.result-actions { display: flex; gap: 8px; justify-content: center; flex-wrap: wrap; }
.result-actions button, .share-btn { padding: 12px 16px; border-radius: 12px; border: 1px solid #ddd; background: #fff; }
.share-btn { margin-top: 12px; width: 100%; background: var(--brand); color: #fff; border: 0; font-weight: 700; }
.history-view { position: fixed; inset: 0; background: #fff; padding: 16px; }
.history-view ul { list-style: none; padding: 0; }
.history-view li { display: flex; gap: 16px; padding: 12px 0; border-bottom: 1px solid #f0f0f0; }
.history-view li.today { font-weight: 700; }
```

- [ ] **Step 4: 타입체크 + 개발 서버 + 커밋**

Run: `npx tsc --noEmit && npm run dev`
Expected: 타입 에러 없음, 개발 서버 기동
```bash
git add -A
git commit -m "feat: assemble app screens (home/result/history) and styles"
```

---

## Task 16: 샌드박스 통합 검증 & 빌드

**Files:** 없음(검증)

- [ ] **Step 1: 샌드박스 앱에서 전체 플로우 검증**

샌드박스 앱(iOS 시뮬레이터/실기기 또는 Android)에서 스킴으로 앱을 열고 확인:
- 카테고리 선택 → 돌리기 → 릴 순차 정지 + **햅틱** 동작
- 결과 → "먹었어요 기록" → 재실행 시 히스토리 유지(저장소 영구성)
- 확률 가중치: 어제 먹은 메뉴가 덜 나오는지(여러 번 시도)
- 공유 버튼 → 토스 공유 시트
- 주간 히스토리 화면

> 햅틱/공유/저장소 함수명이 폴백만 동작하면 Task 7/9/12의 `require` 바인딩을 정적 `import`로 교체하고 실제 함수명으로 수정 후 재검증.

- [ ] **Step 2: 단위 테스트 + 빌드**

```bash
npm test
npm run build
```
Expected: 테스트 PASS, `dist/`(또는 `.ait`) 산출물 생성

- [ ] **Step 3: 커밋**

```bash
git add -A
git commit -m "chore: sandbox-verified build"
```

---

## Task 17: 콘솔 등록 & 출품 (수동 절차)

**Files:** `granite.config.ts` (최종 일치)

- [ ] **Step 1: 앱인토스 콘솔에 앱 등록**

`https://apps-in-toss.toss.im/` 콘솔에서 미니앱 생성: 한국어 이름 "오늘 뭐먹지 슬롯", `appName`(= `ohneul-slot`), 설명, 아이콘 업로드, 필요한 권한(저장소/공유/햅틱/푸시) 신청.

- [ ] **Step 2: `granite.config.ts`를 콘솔 값과 최종 일치**

`appName`, `brand.displayName`, `brand.icon`, `permissions`를 콘솔 등록값과 정확히 맞춘다.

- [ ] **Step 3: 첫 빌드 제출 & 신청폼 작성**

- 콘솔에서 첫 빌드 업로드(`npm run build` 산출물) — **6/30 이전 필수**
- 챌린지 신청폼 작성: 한국어 앱 이름 / `appName` / 설명 / 주제("일상이 편해지는 순간") 연관성

- [ ] **Step 4: 최종 커밋 & 태그**

```bash
git add -A
git commit -m "release: 오늘 뭐먹지 슬롯 v1 (challenge submission)"
git tag v1.0.0-challenge
```

---

## 자가 점검 결과 (작성자 체크)

- **스펙 커버리지**: 메뉴풀(T3)·슬롯연출+햅틱(T9,T10)·공유(T12)·확률가중치(T5)·히스토리(T4,T13)·푸시(T14)·즐겨찾기(T8,ResultCard)·WebView/granite(T0)·테스트(T1~T6)·출시(T16,T17) — 스펙 9개 기능 모두 태스크에 매핑됨.
- **플레이스홀더**: 순수 로직(T2~T7)은 실제 테스트+구현 코드 포함. 플랫폼 함수명(햅틱/공유/푸시)은 "확인 후 채움"으로 명시 — 실제 시그니처를 추측해 잘못 적기보다 설치된 타입/예제에서 확인하는 단계를 둠.
- **타입 일관성**: `Menu/HistoryEntry/Settings/CategoryId/Rng`(T2)를 이후 태스크가 동일하게 사용. `weightedPick`/`getCandidates`/`weekEntries`/`recentMenuIds` 시그니처가 App 조립(T15)과 일치.
- **알려진 리스크**: 플랫폼 API 정적 import vs 동적 require 번들 동작 — T16에서 검증/교체. 메뉴풀 250개 확장은 기계적 데이터 입력.
